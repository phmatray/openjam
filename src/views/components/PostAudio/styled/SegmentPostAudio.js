// @flow

import styled from 'styled-components';

import SegmentPost from 'views/components/PostText/styled/SegmentPost';

const SegmentPostAudio = styled(SegmentPost).attrs({
  color: 'orange',
})``;

export default SegmentPostAudio;
