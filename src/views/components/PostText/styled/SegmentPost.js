// @flow

import styled from 'styled-components';
import { Segment } from 'semantic-ui-react';

const SegmentPost = styled(Segment).attrs({
  width: '100%',
  margin: 'auto',
  display: 'flex',
  color: 'blue',
})``;

export default SegmentPost;
