import styled from 'styled-components';

const Wrapper = styled.div`
  position: relative;
  display: flex;
  padding: 0 !important;
  margin: 0.2em 0;
  width: 3em;
  height: 3em;
  border-radius: 5%;
  overflow: hidden;
`;

export default Wrapper;
