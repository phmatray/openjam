// @flow

import styled from 'styled-components';

const AlternateEdit = styled.span`
  font-family: Comfortaa, 'Helvetica Neue', Arial, Helvetica, sans-serif;
  font-size: 0.8em;
  font-style: italic;
  font-weight: 400;
  color: #ccc;
`;

export default AlternateEdit;
