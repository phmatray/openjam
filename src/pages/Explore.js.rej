diff a/src/pages/Explore.js b/src/pages/Explore.js	(rejected hunks)
@@ -55,34 +55,34 @@ export class Explore extends Component {
-        )}
-        {artists !== null && (
-          <Section
-            title={t('pages.explore.artists')}
-            items={artists.slice(0, 16)}
-            to="/artists"
-            maxHeight={512}
-          />
-        )}
-        {playlists !== null && (
-          <Section
-            title={t('pages.explore.playlists')}
-            items={playlists.slice(0, 8)}
-            to="/playlists"
-            maxHeight={512}
-          />
-        )}
-        {albums !== null && (
-          <Section
-            title={t('pages.explore.albums')}
-            items={albums.slice(0, 16)}
-            to="/albums"
-            maxHeight={512}
-          />
-        )}
-        {labels !== null && (
-          <Section
-            title={t('pages.explore.labels')}
-            items={labels.slice(0, 16)}
-            to="/labels"
-            maxHeight={512}
-            showDivider={false}
-          />
-        )}
+          )}
+          {artists !== null && (
+            <Section
+              title={t('pages.explore.artists')}
+              items={artists.slice(0, 16)}
+              to="/artists"
+              maxHeight={512}
+            />
+          )}
+          {playlists !== null && (
+            <Section
+              title={t('pages.explore.playlists')}
+              items={playlists.slice(0, 8)}
+              to="/playlists"
+              maxHeight={512}
+            />
+          )}
+          {albums !== null && (
+            <Section
+              title={t('pages.explore.albums')}
+              items={albums.slice(0, 16)}
+              to="/albums"
+              maxHeight={512}
+            />
+          )}
+          {labels !== null && (
+            <Section
+              title={t('pages.explore.labels')}
+              items={labels.slice(0, 16)}
+              to="/labels"
+              maxHeight={512}
+              showDivider={false}
+            />
+          )}
